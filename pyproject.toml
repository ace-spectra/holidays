[project]
name = "holidays-api"
version = "0.1.0"
description = "Free public and bank holiday data API."
requires-python = ">=3.13"
dependencies = [
    "fastapi",
    "pyyaml",
]

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk",
]